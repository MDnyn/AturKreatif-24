title: Certifi-Guess

You have intercepted a (server.crt) from a suspicious server. We've also discovered a server.key file that could serve multiple purposes. Server.key associated with the certificate (server.crt) can be used to establish a secure connection (e.g., SSL/TLS) and sign any documents. 

Search for anomalies within it that might be the flag you're looking for.

